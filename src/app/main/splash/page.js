import Splash from 'src/sections/main/splash/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Upright',
};

export default function LoginPage() {
  return <Splash />;
}